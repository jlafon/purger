-remove
exception list
warn only empty dirs 
-append "/" to dirs
special case for last minute re-emerging users
-expired_files.txt needs to included already warned but not deleted files
-add timestamp to expired_files.txt
panfs specific query in prm and treewalk
hash/load balancer for volumes
better error checking and logging on open/close (notice file)
utility to see how many epired files there are

security model:
treewalk can ALL on snapshot
treewalk can ALL on expired_files
chkscratch can READ on snapshot
chkscratch can READ on exception
admin can ALL on ALL (?) (**exception)

more test cases/programs
manage exception list
